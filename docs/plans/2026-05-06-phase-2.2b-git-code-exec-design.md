# Phase 2.2b — `:git/*` handlers + `:code/run` + `_observe` widening

**Status:** DESIGN — pending ARIS R0
**Date:** 2026-05-06
**Branch (worktree):** `feat/v0.9-2.2b-git-code-exec` off `feat/v0.9-persistence-coder@e9669e7` (post 2.2a merge `4e8eeaf` + post-merge follow-up `e9669e7`)
**Calendar target:** ~2-3 working days (2026-05-08 → 2026-05-11)
**Privacy mandate:** NOT pushed to origin. ZERO Co-Authored-By trailer in commits or this doc.
**ARIS standard:** mean ≥ 8.0 / min ≥ 7.5 = PASS. Codex `--reasoning high` + 5-file scope + verdict-block-first template.

## 1. Overview

Phase 2.2b adds the next batch of audit-anchored substrate ops the persistence-coder ReAct loop needs to be useful: `:git/{diff, status, log, commit}` + `:code/run`. It also closes a load-bearing gap in 2.2a's `_observe` rendering that left the LLM blind on its own past tool outputs.

This phase is the LLM-meets-substrate moment. After 2.2b ships, the coder can read code (`:fs/read` from 2.2a), shell out (`:shell/exec` from 2.2a), execute Python in the soft-isolation sandbox (`:code/run`, new), and inspect/commit git state (`:git/*`, new) — and **see its own outputs** in the next iteration's prompt.

Phase 2.2b explicitly does NOT widen the substrate beyond these ops. Plan AST escalation (2.3a), MCTS branch (2.3b), and REPL pause (2.3d) remain queued.

## 2. Locked design decisions (LD1–LD3)

Locked via 4 codex high adversarial passes during brainstorming (2026-05-05). Process: receipts via Serena scan → controller proposal → adversarial codex (argue against) → fold or defend → iterate → lock.

### LD1 — `:code/run` is a new op, not a re-wiring of `:code/exec`

**Decision:** Add `:code/run` to `CANONICAL_AUDIT_WRAPPED_OPS` only (NOT `RAW_OPS`). New handler `make_code_run_dispatch_handler(*, project_root: Path, scratch_dir: Path) -> Handler` installed at `position="bottom"` by `__main__.py`, mirroring 2.2a's `:fs/*` install pattern. Single-mode substantive-return: clause runs subprocess inline + computes hashes + returns `{stdout, stderr, exit_code, wall_clock_ms, output_hash}` dict.

**Legacy `:code/exec` + `exec_code()` + 12+ tests in `tests/effect/test_code_exec.py` + the F4 strict-xfail are completely UNCHANGED.** This is the load-bearing decider — the F4 strict-xfail is the v0.9.x real-OS-sandbox acceptance signal; preserving it dominates aesthetic concerns about "two ops for one logical effect."

**Why a new op name (rejected: shared `:code/exec` op):** A shared-name dispatch handler would need dual-mode args-shape switching (`source` vs `source_hash` keys) — a footgun where correctness depends on a negative guarantee (never call `k(args)` for one branch). Accepted tradeoff: future audit-replay queries for "all code execution events" must union `:code/exec OR :code/run`.

**Code reuse:** Extract `_run_subprocess` and `_output_hash` from `code.py` as shared helpers (rename `_run_subprocess` → `run_subprocess`; `_output_hash` → `output_hash`; both stay in `code.py`). Both `exec_code` (legacy) and the new `:code/run` handler call them. Keeps timeout/env/subprocess-spawn semantics identical across both ops by SHARED IMPLEMENTATION, not by convention.

**Op shape:**
```
:code/run args:
  source: str            (required; Python source to execute)
  stdin: str = ""        (optional; piped to subprocess stdin)
  timeout_seconds: float = 5.0
  memory_mb: int = 128

:code/run result:
  stdout: str
  stderr: str
  exit_code: int
  wall_clock_ms: int
  output_hash: str       ("sha256:<hex>" of canonical-JSON of {stdout, stderr, exit_code})
```

**Audit chain:** `:code/run` AuditEntry has `args_hash = canonical_hash({"source", "stdin", "timeout_seconds", "memory_mb"})` (source-shape; differs from legacy `:code/exec` audit which is hash-shape). `result_hash = canonical_hash(<result dict>)`.

### LD2 — `:git/*` is a thin wrapper over `:shell/exec`, not a parallel native handler

**Decision:** Add four `:git/*` ops to `CANONICAL_AUDIT_WRAPPED_OPS` only: `:git/diff`, `:git/status`, `:git/log`, `:git/commit`. New handler `make_git_handler(*, project_root: Path) -> Handler` with quartet `wraps={":git/diff", ":git/status", ":git/log", ":git/commit"}` mirroring `make_fs_handler` factory pattern. Each clause:

1. validates the op-specific args dict against the schema below
2. enforces cwd confinement: `cwd.resolve().is_relative_to(project_root.resolve())` via `_safe_resolve` helper extracted from `fs.py` (or duplicated inline — implementer's call)
3. constructs argv from validated args
4. delegates execution via `s.effect.perform(":shell/exec", {argv, cwd, allowlist_version: shell.ALLOWLIST_VERSION, env_allowlist_subset, timeout_s})`
5. passes through the result shape from `:shell/exec` (`{exit, stdout, stderr, wall_clock_ms}`) augmented with op-specific extras (e.g. `:git/commit` adds `sha` parsed from porcelain output)

**Excluded ops** (out of 2.2b scope): `:git/{push, pull, fetch, checkout, branch, merge, reset, rebase, tag, stash, cherry-pick}`. `:git/push` is forbidden by privacy mandate. The other ref-state mutations are out of 2.2b — branching for MCTS lands in 2.3b via `s.txn.fork`, not git refs.

**Why thin wrapper (rejected: native handler with own subprocess):** A native handler would duplicate ~60 LOC of timeout/env/subprocess/error-shaping that already lives in `shell.py:81-103` (including the macOS bytes-branch for `TimeoutExpired` discovered in 2.2a). Divergence risk is real and unbounded. The thin wrapper inherits `:shell/exec`'s tested execution path. Defense-in-depth (cwd confinement, narrow op shapes) lives at the wrapper layer, where the LLM's intent is shaped before it hits the shared shell substrate.

**Why narrow op shapes (rejected: arbitrary git flag passthrough):** Bans on `--amend` / `-S` / `--no-verify` / `--allow-empty` are arbitrary policy in code that creates a compatibility burden if ever relaxed. Narrow op shapes enforce safety BY CONSTRUCTION — the schema simply doesn't model those degrees of freedom, so there's nothing to relax. If a future workflow legitimately needs `--amend`, it's a separate v0.9.x op (e.g. `:git/amend`), not a flag toggle on `:git/commit`.

**Op shapes:**
```
:git/diff args:
  ref: str = "HEAD"          (optional; tree-ish to diff against)
  paths: list[str] = []       (optional; resolved relative to cwd)
  cached: bool = False        (optional; --cached flag)
  cwd: str = <project_root>   (must be is_relative_to project_root)

:git/diff result:
  exit: int
  stdout: str       (the diff)
  stderr: str
  wall_clock_ms: int

:git/status args:
  paths: list[str] = []       (optional; specific paths; default = repo-wide)
  cwd: str = <project_root>

:git/status result:
  exit: int
  stdout: str       (porcelain v1 output)
  stderr: str
  wall_clock_ms: int

:git/log args:
  n: int = 10                 (--max-count)
  paths: list[str] = []       (optional; specific paths)
  format: str = "oneline"     (allowed: "oneline" | "short" | "medium")
  cwd: str = <project_root>

:git/log result:
  exit: int
  stdout: str
  stderr: str
  wall_clock_ms: int

:git/commit args:
  message: str               (required; non-empty)
  paths: list[str]           (required; non-empty; each is_relative_to project_root)
  cwd: str = <project_root>

:git/commit result:
  exit: int
  stdout: str       (porcelain output)
  stderr: str
  wall_clock_ms: int
  sha: str | None   (parsed from porcelain commit-id line; None if commit failed)
```

**Audit chain (TWO entries per `:git/*` op):**
- One `:git/<sub>` AuditEntry with `args_hash = canonical_hash(<op args>)` — high-level intent.
- One `:shell/exec` AuditEntry with `args_hash = canonical_hash({argv, cwd, allowlist_version, env_allowlist_subset, timeout_s})` — actual execution.
- Both byte-identity-stable across replay. Replay queries can key off either layer.
- Documented in `CHANGELOG-effect.md` as a known design property (NOT a deviation).

**Known constraint** (must be documented in `CHANGELOG-effect.md` § "Known limitations"): `:git/*` clauses recursively call `s.effect.perform(":shell/exec", ...)`. Per `EffectInIoBlock`, raw `effect.perform()` is forbidden inside `dosync`. Therefore `:git/*` ops MUST be dispatched from outside any active `dosync` (i.e., from `_act` or other top-level dispatchers — never queued via `tx.effect()` from inside dosync replay). 2.3a Plan AST execution will need to respect this.

### LD3 — `build_messages` adds a "Latest action output" section

**Decision:** Modify `src/persistence/coder/_prompt.py:build_messages` to add a NEW section between the task header and the "Recent loop history" block. The section is omitted on iter 0 (when `obs.recent_actions` is empty); otherwise it pulls `obs.recent_actions[-1]` and renders it with explicit field formatting:

```
Latest action output (iter <N>):
  op: <op-string>
  error: <error-string or "none">
  exit_code: <int (if present in result_summary)>
  stdout: |
    <full _summarize_result-truncated stdout, ~600 chars max>
  stderr: |
    <full _summarize_result-truncated stderr, ~600 chars max>
  matches: count=<N>, first_3=[<first 3 entries>]   (only for list-shaped result fields like :fs/glob)
  <other-keys>: <repr-truncated-to-200>
```

Older history entries (iters before the latest) keep the existing `[:200]` cap on `json.dumps(d, sort_keys=True)`. This preserves prompt-cost control while guaranteeing the LLM sees its most recent action's full output.

**Why CHANGE (rejected: NO_CHANGE):** Receipts step (Serena scan) revealed a SECOND truncation in `build_messages` (line 54-58) that the controller initially missed. Math: a `:act/result` JSON-encoded with `sort_keys=True` has alphabetical keys `args_hash, error, latency_ms, op, result_summary` — the first 200 chars never reach `result_summary.stdout`. Concrete failure case: a `:code/run` with a 10KB Python traceback in `result_summary.stderr` is INVISIBLE to the LLM in iter+1's prompt. The LLM cannot fix errors it cannot see. This is "blind on its own outputs" — a structural failure of the ReAct loop, not an edge case.

**Why explicit-field rendering for the latest action (rejected: just bump `[:200]` to `[:1500]` universally):** A universal cap would (a) inflate prompt cost on every history entry regardless of relevance, (b) still not solve list-shaped results like `:fs/glob`'s `matches: list[str]` which can be hundreds of items. Explicit-field rendering with shape-aware collapsing (count + first 3 for lists) gives bounded prompt cost AND full visibility on the last action's stdout/stderr.

**Acceptance signal (load-bearing):** unit test `tests/coder/test_observe_latest_action.py::test_latest_action_stdout_visible_in_prompt` constructs an `Observation` with one action whose `result_summary.stdout` contains the sentinel string `"TRACEBACK_SENTINEL_2_2_B"`. Asserts `build_messages(...)[0]["content"]` contains the sentinel verbatim. If this fails, we picked wrong (because the whole point of LD3 is to fix the blind-on-own-outputs failure).

## 3. Module layout

### New files

| File | Purpose | LOC |
|---|---|---|
| `src/persistence/effect/handlers/git.py` | `make_git_handler(*, project_root) -> Handler`; per-op clauses for `:git/{diff,status,log,commit}`. Each clause validates args, enforces cwd confinement, constructs argv, delegates via `s.effect.perform(":shell/exec", ...)`, augments result with op-specific extras (e.g. `sha` for commit). | ~200 |
| `tests/effect/handlers/test_git_handler.py` | G1 — per-op tests + capability denial + Hypothesis property + audit-byte-identity. | ~12 tests |
| `tests/effect/handlers/test_code_run_handler.py` | G2 — `:code/run` per-op tests + audit-chain wiring + replay byte-identity. Separate from existing `tests/effect/test_code_exec.py` which stays untouched. | ~6 tests |
| `tests/coder/test_act_git.py` | G3 — coder dispatches `:git/diff` + `:git/commit` via `_act`, asserts `:act/result` datom written, audit chain advances by 2 entries (`:git/<sub>` + `:shell/exec`). | ~4 tests |
| `tests/coder/test_act_code_run.py` | G4 — coder dispatches `:code/run` via `_act`, asserts `:act/result` + audit advances by 1 entry. | ~4 tests |
| `tests/coder/test_observe_latest_action.py` | G5a — LD3 acceptance signal (sentinel test) + list-shape collapsing test for `:fs/glob` matches + non-string field rendering. | ~5 tests |

### Modified files

| File | Change |
|---|---|
| `src/persistence/effect/handlers/code.py` | Extract `_run_subprocess` → `run_subprocess` (public) and `_output_hash` → `output_hash` (public). Add `make_code_run_dispatch_handler(*, project_root, scratch_dir) -> Handler` factory. Legacy `exec_code()` body unchanged but now calls `run_subprocess` (already does — pure rename). |
| `src/persistence/effect/_audit_stack.py` | Add `:code/run`, `:git/diff`, `:git/status`, `:git/log`, `:git/commit` to `CANONICAL_AUDIT_WRAPPED_OPS` (NOT `RAW_OPS`). |
| `src/persistence/effect/handlers/__init__.py` | Re-export `make_git_handler`, `make_code_run_dispatch_handler`. |
| `src/persistence/coder/__main__.py` | After `Substrate.open()`, install `make_git_handler(project_root=...)` and `make_code_run_dispatch_handler(project_root=..., scratch_dir=...)` at `position="bottom"`. (Same install pattern as 2.2a's `make_fs_handler`.) |
| `src/persistence/coder/_prompt.py` | LD3 — add "Latest action output" section to `build_messages`. New helper `_render_latest_action(action: dict) -> list[str]`. |
| `tests/coder/test_loop_e2e.py` | Extend with G6 — 5-iter scripted scenario: iter 1 `:fs/read`, iter 2 `:code/run`, iter 3 `:git/diff`, iter 4 `:git/commit`, iter 5 `done=true`. Asserts exactly 5 `:llm/decision` + 4 `:act/result` + 9 audit entries (5 `:llm/call` + 1 `:fs/read` + 1 `:code/run` + 1 `:git/diff`+`:shell/exec` pair counted as 2 + 1 `:git/commit`+`:shell/exec` pair counted as 2 = total 9). |
| `tests/effect/test_canonical_audit_stack.py` | G7 — drift-pin update for new ops in `WRAPPED_OPS`. |
| `src/persistence/effect/CHANGELOG-effect.md` | Phase 2.2b entry: new ops + audit-chain double-entry semantics + `EffectInIoBlock` constraint on `:git/*`. |
| `src/persistence/coder/CHANGELOG-coder.md` | Phase 2.2b entry: `:code/run` + `:git/*` dispatch in `_act`; LD3 prompt widening. |

## 4. Test gates G1–G7

| Gate | Test file | New tests | What it asserts |
|---|---|---|---|
| G1 | `tests/effect/handlers/test_git_handler.py` | ~12 | Each `:git/*` op runs against a real tmp-repo. Cwd-outside-project_root rejected with hard validation error (NOT a git error string). `:git/commit{message: "", paths: ["x"]}` rejected. `:git/commit{message: "msg", paths: []}` rejected. Hypothesis property: arbitrary message/paths round-trip through canonical_hash deterministically. |
| G2 | `tests/effect/handlers/test_code_run_handler.py` | ~6 | `:code/run` runs in soft-isolation sandbox (reuses `run_subprocess` so soft-isolation guarantees from 2.0b carry forward). Two runs of same source produce byte-identical `output_hash`. Audit middleware emits `:code/run` AuditEntry with source-shape `args_hash`. Forbidden-import path returns non-zero exit + sentinel in stderr (same as `exec_code`). |
| G3 | `tests/coder/test_act_git.py` | ~4 | Coder's `_act` dispatching `{op: ":git/diff", args: {...}}` triggers handler, writes `:act/result` datom with `result_summary` truncated by `_summarize_result`, advances audit chain by 2 entries. Same for `:git/commit` — sha appears in result. |
| G4 | `tests/coder/test_act_code_run.py` | ~4 | Coder's `_act` dispatching `{op: ":code/run", args: {source: "..."}}` returns dict, writes `:act/result`, audit chain advances by 1 entry. Source-with-traceback case: stderr appears in `result_summary` (truncated to ~600 chars). |
| G5a | `tests/coder/test_observe_latest_action.py` | ~5 | LD3 sentinel test (load-bearing). List-shape collapsing for `:fs/glob` matches. Non-string field rendering. iter-0 omits the section. Iter-N>0 shows it. |
| G5b (extends G5 from 2.2a) | `tests/coder/test_loop_e2e.py` | ~3 (added to existing 5) | 5-iter scripted scenario with full audit-chain count assertion (9 entries). 4-iter with `:code/run` exit_code != 0 — coder sees the stderr in iter 5's prompt (proves LD3 wired through e2e). |
| G6 | `tests/coder/test_loop_replay.py` | ~3 (added to existing 3) | New ops byte-identity replay. `:code/run` replay across two runs. `:git/diff` replay across two runs (deterministic argv from same args). |
| G7 | `tests/effect/test_canonical_audit_stack.py` | minor | Drift-pin asserts new ops are in `WRAPPED_OPS` and NOT in `RAW_OPS`. |

**Total new tests target:** ~30-35 (12 G1 + 6 G2 + 4 G3 + 4 G4 + 5 G5a + 3 G5b + 3 G6 + minor G7 = ~37). Suite expected to grow from 2363 → ~2400.

## 5. Sequencing (LD7 from 2.2a — substrate prereqs FIRST)

1. **T1** — Drift-pin update (`_audit_stack.py`) + `code.py` helper extraction (`run_subprocess`, `output_hash` made public). G7 + tests for the rename.
2. **T2** — `make_code_run_dispatch_handler` (`code.py` additions) + G2 tests.
3. **T3** — `make_git_handler` (`git.py` new file) + G1 tests.
4. **T4** — `__main__.py` wires both handlers at `position="bottom"`. G3 + G4 tests for coder dispatch.
5. **T5** — `build_messages` LD3 widening (`_prompt.py` + `_render_latest_action`). G5a tests including the sentinel test.
6. **T6** — Extend `test_loop_e2e.py` with G5b 5-iter scenario.
7. **T7** — Extend `test_loop_replay.py` with G6 byte-identity tests for new ops.
8. **T8** — CHANGELOGs (effect + coder) + lockfile re-snapshot if needed.
9. **T9** — Final ARIS Impl R1 codex review on touched files.

T1-T3 are substrate-only; coder body changes (T4-T6) come after.

## 6. Forced spec deviations to expect

Calibration: 2.1b had 5 deviations, 2.1c had 9, 2.1c.6 had 3, 2.2a had 4. Phase 2.2b expected ~3-5 because the design surface is smaller (one new module + one widening + one extracted helper).

Likely hotspots — leave room in each task to verify substrate APIs against actual behavior before committing to spec'd usage:

1. **`s.effect.perform` from inside a handler clause** — LD2's `:git/*` wrapper performs `:shell/exec` internally. Verify this works under the runtime (no `EffectInIoBlock` raised when the outer perform is from `_act` outside dosync). If `EffectInIoBlock` fires anyway, fall back to direct subprocess call in `:git/*` clauses (loses the LD2 benefit; would force a re-design if hit).
2. **`:shell/exec` argv-construction edge cases** — `:git/log -n 10 --pretty=oneline -- foo bar` requires the right argv ordering. Validate against tests.
3. **`canonical_hash` on result shape** — `:git/commit`'s `sha` field is `str | None`. Verify `canonical_hash` handles None deterministically (it should; verify).
4. **`_summarize_result` truncation pre-LD3** — interaction with the new "Latest action output" rendering. The summarized string already has truncation markers; LD3 renders without re-summarizing. Confirm no double-truncation.
5. **`output_hash` semantics** — `:code/run` returns `output_hash` BUT also gets a separate audit `result_hash` from the middleware. The two should be related but not identical (output_hash is over `{stdout, stderr, exit_code}`; result_hash is over the full result dict including `wall_clock_ms` and `output_hash` itself). Document the distinction.

Document deviations in commit messages (not just impl plan), per 2.2a precedent.

## 7. Known limitations & W3 honest-rescope deferrals

These are NOT closed by 2.2b; they remain queued artifacts:

- **2.1c.5** — caller identity Ed25519 attestation (~1-2 days when dispatched, blocking for non-loopback HTTP consumers).
- **F4 strict-xfail in `tests/effect/test_code_exec.py`** — v0.9.x real-OS-sandbox confidentiality acceptance signal. UNCHANGED by 2.2b. `:code/run` shares the same soft-isolation guarantees as `:code/exec` because they share `run_subprocess`. When v0.9.x lands, BOTH legacy `:code/exec` and the new `:code/run` automatically get the boundary upgrade.
- **2.4a `:loop/exhausted` sentinel + `:sys/now`/`:sys/random` clock/random** — telemetry-driven, deferred.
- **`tx` extraction race** under concurrent HTTP serving (pre-existing from 2.1c).
- **`:shell/exec` cwd confinement at the `:shell/exec` layer** — LD2 puts confinement at the `:git/*` wrapper layer (the right altitude for git semantics), but `:shell/exec` itself remains unconstrained. If/when a future op set needs cwd confinement on shell directly, that's a separate `:shell/exec` v2 design (allowlist-version-bumping change).
- **Audit-replay queries for "all code execution"** — must union `:code/exec OR :code/run`. Accepted cost of LD1's namespace separation.
- **`:git/*` cannot be queued via `tx.effect()` inside dosync replay** — `EffectInIoBlock` constraint. 2.3a Plan AST will need to respect.

## 8. Effort target

~2-3 working days, ~600-800 LOC across 1 new handler + 1 helper-extraction + 1 prompt-builder widening + tests + CHANGELOGs. Smaller than 2.2a (~1,600 LOC) because:
- `:git/*` is a thin wrapper, not a new subprocess implementation.
- `:code/run` reuses `run_subprocess` (extracted, not new).
- LD3 is ~30 LOC of prompt-builder code + ~5 small tests.

Test-count target ~30-35 new tests (vs 2.2a's +63). Suite 2363 → ~2400 expected.

## 9. Critical-path posture

2.2b SHIPPED → 2.3a (Plan AST escalation) → 2.3b (MCTS branch + `s.txn.fork`) → 2.3d (REPL pause) → 2.4a-d harden → 2.4c lockfile + `tests/preflight_manifest.toml` (~Fri 2026-06-12, cross-track precursor from `mimir-os-product`) → `v0.9.0a1` tag (by 2026-06-14). Hard cutoff Phase 2: 2026-06-05; ~5wk margin to Phase E launch 2026-07-20 holds.

## 10. Brainstorm-time ARIS lineage

- **Codex consensus pattern (NEW for this phase)** — instead of a single ARIS R0 review at design-time, each architectural decision (LD1, LD2, LD3) was locked individually via codex high adversarial review during brainstorming. 4 codex high passes total: Q1 round 1 (NEW, controller pushed back), Q1 round 2 (C-revised, converged), Q2 (B2, controller folded), Q3 (CHANGE, converged). The pattern is being formalized as a user-level skill `consensus-driven-architecture-decision` (see `~/.claude-nawfal-2/skills/consensus-driven-architecture-decision/SKILL.md`).
- **Design R0 (codex `--reasoning high`):** scheduled after this doc commits. Expected to validate the LD1/LD2/LD3 decisions and surface ~2-4 BLOCKING/IMPORTANT findings per 2.1b/2.1c/2.2a calibration.
- **Design R1 (if R0 PASS-WITH-FIXES):** fold residuals, freeze design.

## 11. Persistence channels

- Conductor: `~/Projects/ai-box/conductor/tracks/persistence-os-product_20260429/STATUS.md` — append 2026-05-06 block when this doc commits.
- Auto-memory: `project_persistence_os_phase_2.2b_*` (when shipped).
- Serena: `persistence-os/v0.9-phase-2.2b-*` (when shipped).
- Vault: `nawfal-dev/L1` tx (when shipped).
