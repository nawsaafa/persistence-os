"""Persistence OS — bitemporal, effect-typed cognitive runtime.

Modules:
    fact     — bitemporal 8-tuple datom store (this release)
    effect   — algebraic effect handler stack (stub)
    plan     — EDN plan AST + skill library (stub)
    replay   — counterfactual trajectory engine (stub)
    txn      — software transactional memory (stub)
    spec     — parse-don't-validate boundary contracts (stub)
    repl     — live production inspection (stub)
"""

__version__ = "0.1.0a1"
