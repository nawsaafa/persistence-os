(:chain
  :name "pause-resume-sysnow-demo"
  :description "G5 cap-4+cap-5 base — :llm/call exercises cap-1+cap-3; pause/resume + :sys/now exercised by G5 harness directly"
  :steps [(:step :id 1
                :op :llm/call
                :args {:model "echo" :messages [{:role "user" :content "warmup"}]}
                :capability (:Capability :op "coder" :qualifier "read"))])
